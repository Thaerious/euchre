[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "euchre"
version = "1.3.1"
dependencies = [
    "typeguard",
    "colorama"
]

[tool.setuptools.packages]
find = {where = ["src"]}